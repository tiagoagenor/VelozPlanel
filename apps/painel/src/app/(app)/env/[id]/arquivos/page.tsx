import { FolderOpen } from "lucide-react";
import { ComingSoon } from "@/components/ComingSoon";

export default function EnvArquivosPage() {
  return (
    <ComingSoon
      icon={FolderOpen}
      title="Arquivos"
      description="Um gerenciador de arquivos para enviar, editar e organizar o conteúdo do ambiente direto pelo painel. Em breve por aqui."
    />
  );
}
